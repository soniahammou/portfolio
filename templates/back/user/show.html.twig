{% extends 'back/base.html.twig' %}


{% block body %}
    <h1>User</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user.id }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ user.email }}</td>
            </tr>
            <tr>
                <th>Roles</th>
                <td>{{ user.roles ? user.roles|json_encode : '' }}</td>
            </tr>
            <tr>
                <th>Password</th>
                <td>{{ user.password }}</td>
            </tr>
            <tr>
                <th>IsVerified</th>
                <td>{{ user.isVerified ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>
    <a href="{{path('app_back_user_index') }}"><button type="button" class="btn btn-primary"> Retour à la liste</button></a>
    <a href="{{path('app_back_user_edit', {'id': user.id}) }}"><button type="button" class="btn btn-primary"> Modifier</button></a>



    {{ include('back/user/_delete_form.html.twig') }}
{% endblock %}
